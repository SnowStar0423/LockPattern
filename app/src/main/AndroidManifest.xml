<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.henleylee.lockpattern.demo">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PatternCreateActivity"
            android:label="@string/pattern_create"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PatternCheckActivity"
            android:label="@string/pattern_check"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PatternSettingActivity"
            android:label="@string/pattern_setting"
            android:screenOrientation="portrait" />
    </application>

</manifest>